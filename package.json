{
  "name": "d9network-ai-business-growth-platform",
  "version": "1.0.0",
  "private": true,
  "description": "The D9Network AI Business Growth Platform",
  "scripts": {
    "dev": "npx --yes netlify-cli@18.1.0 dev",
    "preview": "node scripts/serve-static.js",
    "build": "node scripts/build.js",
    "check": "node scripts/check-config.js",
    "test": "node --test"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=20"
  }
}
